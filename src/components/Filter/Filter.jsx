import PropTypes from 'prop-types';
import css from './Filter.module.css';

export const Filter = () => {
	return (
	<div className={css.filter_wrapper}>
		Filterlist
	</div>
	);
}

Filter.propTypes = {

};